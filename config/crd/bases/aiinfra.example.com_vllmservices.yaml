---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: vllmservices.aiinfra.example.com
spec:
  group: aiinfra.example.com
  names:
    kind: VLLMService
    listKind: VLLMServiceList
    plural: vllmservices
    singular: vllmservice
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VLLMService is the Schema for the vllmservices API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of VLLMService
            properties:
              gatewayRef:
                description: GatewayRef 表示当前VLLMService要挂载到哪个Gateway上。 不填写gatewayRef时，operator只创建Deployment和service，不创建HTTPRoute
                properties:
                  host:
                    description: Host是HTTPRoute要匹配的域名。后面创建HTTPRoute时，会写入spec.hostnames
                    minLength: 1
                    type: string
                  name:
                    description: name是要引用的Gateway名称
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace是Gateway所在命名空间。如果不填写，默认认为Gateway和当前VLLMService在同一个命名空间。
                    minLength: 1
                    type: string
                  sectionName:
                    description: SectionName 是要绑定的Gateway listener名称。后面创建HTTPRoute时，会写入parentRefs.sectionName
                    minLength: 1
                    type: string
                required:
                - host
                - name
                - sectionName
                type: object
              image:
                minLength: 1
                type: string
              labels:
                additionalProperties:
                  type: string
                type: object
              modelName:
                minLength: 1
                type: string
              modelPath:
                minLength: 1
                type: string
              nodeSelector:
                additionalProperties:
                  type: string
                type: object
              port:
                default: 8000
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              replicas:
                default: 1
                format: int32
                minimum: 1
                type: integer
              resources:
                description: ResourceRequirements describes the compute resource requirements.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              runtimeClassName:
                type: string
              schedulerName:
                type: string
              storage:
                description: |-
                  EDIT THIS FILE!  THIS IS SCAFFOLDING FOR YOU TO OWN!
                  NOTE: json tags are required.  Any new fields you add must have json tags for the fields to be serialized.
                properties:
                  mountPath:
                    minLength: 1
                    type: string
                  pvcName:
                    minLength: 1
                    type: string
                  readOnly:
                    default: true
                    type: boolean
                  subPath:
                    type: string
                required:
                - mountPath
                - pvcName
                type: object
            required:
            - image
            - modelName
            - modelPath
            - resources
            - storage
            type: object
          status:
            description: status defines the observed state of VLLMService
            properties:
              deploymentName:
                type: string
              gatewayRefName:
                description: GatewayRefName 表示当前VLLMService引用的Gateway名称。 注意： 这不是operator创建的Gateway，而是引用
                type: string
              gatewayRefNamespace:
                description: GatewayRefNamespace 表示当前VLLMService引用的Gateway所在命名空间
                type: string
              httpRouteName:
                description: HTTPRouteName 表示operator为当前VLLMService创建的HTTPRoute名称
                type: string
              message:
                type: string
              phase:
                type: string
              readyReplicas:
                format: int32
                type: integer
              serviceName:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
